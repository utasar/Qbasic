Write a program to count and display only those records who
study in class 10 from the data file named "record.txt" which
contains Roll number, Name, Address, and Class of several
students.
OPEN "record.txt" FOR INPUT AS #1
CLS
DO WHILE NOT EOF(1)
    INPUT #1, r, n$, a$, c
    IF VAL(c) = 10 THEN
        PRINT r, n$, a$, c
    END IF
LOOP
CLOSE #1
END

        
