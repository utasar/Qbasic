 Count total number of students who passed in all the
subjects:
OPEN "result.doc" FOR INPUT AS #1
CLS
DO WHILE NOT EOF(1)
    INPUT #1, roll, name$, class$, maths, science, english, nepali, computer
    IF maths < 40 OR science < 40 OR english < 40 OR nepali < 40 OR computer < 40 THEN
        PRINT name$
    END IF
LOOP
CLOSE #1
END
        
