 Write a program to display all the records from the data file
named "record.txt" which contains Roll number, Name,
Address, and Class of several students.
OPEN "record.txt" FOR INPUT AS #1
CLS
DO WHILE NOT EOF(1)
    INPUT #1, r, n$, a$, c
    PRINT r, n$, a$, c
LOOP
CLOSE #1
END

        
